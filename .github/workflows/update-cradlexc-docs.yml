name: Update docs from a CradleXC tag with Copilot

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing CradleXC release tag to analyze
        required: true
        type: string

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: update-cradlexc-docs-${{ inputs.tag }}
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Validate input
        shell: bash
        env:
          TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "::error::Invalid release tag: $TAG"
            exit 1
          fi

      - name: Checkout documentation repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_AW_CI_TRIGGER_TOKEN || github.token }}

      - name: Checkout CradleXC at requested tag
        uses: actions/checkout@v4
        with:
          repository: cradle-lang/CradleXC
          ref: ${{ inputs.tag }}
          path: upstream-cradlexc
          fetch-depth: 0
          token: ${{ secrets.CRADLE_RELEASES_TOKEN }}

      - name: Verify upstream checkout
        shell: bash
        env:
          TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          expected="$(git -C upstream-cradlexc rev-list -n 1 "$TAG")"
          actual="$(git -C upstream-cradlexc rev-parse HEAD)"
          if [[ -z "$expected" || "$actual" != "$expected" ]]; then
            echo "::error::Expected upstream tag $TAG at $expected but checked out $actual."
            exit 1
          fi

      - name: Install GitHub Copilot CLI
        shell: bash
        run: npm install --global @github/copilot@1.0.83

      - name: Synchronize documentation
        shell: bash
        env:
          COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_CLI_TOKEN }}
          TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          if [[ -z "$COPILOT_GITHUB_TOKEN" ]]; then
            echo "::error::COPILOT_CLI_TOKEN is not configured."
            exit 1
          fi

          prompt="$(cat .github/prompts/update-cradlexc-docs.md)

          Requested release tag: $TAG"

          copilot \
            --prompt "$prompt" \
            --no-ask-user \
            --no-custom-instructions \
            --disable-builtin-mcps \
            --secret-env-vars=COPILOT_GITHUB_TOKEN \
            --max-ai-credits 40 \
            --allow-tool write \
            --allow-tool 'shell(cat)' \
            --allow-tool 'shell(cd)' \
            --allow-tool 'shell(date)' \
            --allow-tool 'shell(find)' \
            --allow-tool 'shell(git cat-file:*)' \
            --allow-tool 'shell(git describe:*)' \
            --allow-tool 'shell(git diff:*)' \
            --allow-tool 'shell(git log:*)' \
            --allow-tool 'shell(git merge-base:*)' \
            --allow-tool 'shell(git rev-list:*)' \
            --allow-tool 'shell(git rev-parse:*)' \
            --allow-tool 'shell(git show:*)' \
            --allow-tool 'shell(git show-ref:*)' \
            --allow-tool 'shell(git status)' \
            --allow-tool 'shell(git tag:*)' \
            --allow-tool 'shell(grep)' \
            --allow-tool 'shell(head)' \
            --allow-tool 'shell(ls)' \
            --allow-tool 'shell(pwd)' \
            --allow-tool 'shell(rg)' \
            --allow-tool 'shell(sed)' \
            --allow-tool 'shell(sort)' \
            --allow-tool 'shell(tail)' \
            --allow-tool 'shell(wc)'

      - name: Verify Copilot changed only documentation paths
        id: changes
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t changed < <(
            {
              git diff --name-only
              git ls-files --others --exclude-standard
            } | grep -v '^upstream-cradlexc/$' | sort -u
          )

          for path in "${changed[@]}"; do
            case "$path" in
              docs/*|src/pages/index.tsx|src/components/homepage/*|static/img/home/*)
                ;;
              *)
                echo "::error::Copilot changed a disallowed path: $path"
                exit 1
                ;;
            esac
          done

          echo "count=${#changed[@]}" >> "$GITHUB_OUTPUT"

      - name: Validate documentation
        shell: bash
        run: |
          set -euo pipefail
          npm ci

          mapfile -t markdown_files < <(
            {
              git diff --name-only --diff-filter=ACM -- '*.md' '*.mdx'
              git ls-files --others --exclude-standard -- '*.md' '*.mdx'
            } | grep -v '^upstream-cradlexc/' | sort -u
          )
          if [[ "${#markdown_files[@]}" -gt 0 ]]; then
            npx --yes markdownlint-cli2@0.18.1 "${markdown_files[@]}"
          fi

          npm run build
          git restore --worktree src/data/release-notes.json

      - name: Commit and push automation branch
        shell: bash
        env:
          TAG: ${{ inputs.tag }}
          BRANCH: docs-sync/${{ inputs.tag }}
        run: |
          set -euo pipefail
          printf '%s\n' "$TAG" > .cradlexc-docs-source-tag
          git rm --ignore-unmatch .cradlexc-docs-source-revision

          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git fetch origin \
            "refs/heads/$BRANCH:refs/remotes/origin/$BRANCH" || true
          git switch -C "$BRANCH"
          git add -- \
            .cradlexc-docs-source-tag \
            docs \
            src/pages/index.tsx \
            src/components/homepage \
            static/img/home
          git commit -m "Sync docs with CradleXC $TAG"
          git push --force-with-lease --set-upstream origin "$BRANCH"

      - name: Create or update draft pull request
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GH_AW_CI_TRIGGER_TOKEN || github.token }}
          TAG: ${{ inputs.tag }}
          BRANCH: docs-sync/${{ inputs.tag }}
          CONTENT_CHANGE_COUNT: ${{ steps.changes.outputs.count }}
        run: |
          set -euo pipefail
          body="$(printf '%s\n' \
            "## Summary" \
            "" \
            "Synchronizes the website documentation with CradleXC tag \`$TAG\`. Copilot found $CONTENT_CHANGE_COUNT documentation file change(s), excluding the source-tag marker." \
            "" \
            "## Validation" \
            "" \
            "- Ran Markdown/MDX linting on changed documentation files." \
            "- Ran \`npm run build\`." \
            "- Restricted changes to approved documentation and homepage paths." \
            "" \
            "This draft requires human technical and editorial review before merging.")"

          number="$(gh pr list --state open --head "$BRANCH" --json number --jq '.[0].number // empty')"
          if [[ -n "$number" ]]; then
            gh pr edit "$number" --title "Sync docs with CradleXC $TAG" --body "$body"
          else
            gh pr create \
              --draft \
              --base main \
              --head "$BRANCH" \
              --title "Sync docs with CradleXC $TAG" \
              --body "$body"
          fi
